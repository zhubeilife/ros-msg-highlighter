<idea-plugin>
    <id>org.ros.clion.ros-msg-highlighter</id>
    <name>ROS Interface Syntax Highlighter</name>
    <version>1.0.0</version>
    <vendor url="https://github.com/zhangbeipc">ROS Developer</vendor>

    <description><![CDATA[
        <h2>ROS Interface Syntax Highlighting for CLion</h2>
        <p>
        Provides syntax highlighting for ROS Interface definition files:
        <ul>
            <li><code>.msg</code> - ROS Message definitions</li>
            <li><code>.srv</code> - ROS Service definitions</li>
            <li><code>.action</code> - ROS Action definitions</li>
        </ul>
        </p>
        <h3>Features</h3>
        <ul>
            <li>Syntax highlighting for built-in types (<code>bool</code>, <code>int32</code>, <code>float64</code>, <code>string</code>, etc.)</li>
            <li>Highlighting of package/type references (<code>std_msgs/Header</code>)</li>
            <li>Field name and constant highlighting</li>
            <li>Number, float, and boolean literal highlighting</li>
            <li>Quoted and unquoted string highlighting</li>
            <li>Comment highlighting (<code>#</code> line comments)</li>
            <li>Separator highlighting (<code>---</code>, <code>===</code>, <code>MSG:</code>)</li>
            <li><code>@optional</code> attribute highlighting</li>
            <li>Array bracket matching</li>
            <li>Customizable colors via Settings → Editor → Color Scheme → ROS Interface</li>
        </ul>
        <p>
        Uses the <a href="https://github.com/jtbandes/ros-tmlanguage">ROS Interface TextMate grammar</a>
        via the IntelliJ TextMate Bundles plugin for reliable, high-quality syntax highlighting.
        </p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>1.0.0</h3>
        <ul>
            <li>Initial release</li>
            <li>Syntax highlighting for .msg, .srv, .action files via TextMate bundle</li>
            <li>Customizable color scheme settings</li>
        </ul>
    ]]></change-notes>

    <!-- Dependencies -->
    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.textmate</depends>

    <!-- Extensions -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- File type registration -->
        <fileType
            name="ROSInterface"
            implementationClass="org.ros.clion.highlighter.RosInterfaceFileType"
            fieldName="INSTANCE"
            language="ROSInterface"
            extensions="msg;srv;action"/>

        <!-- Language definition -->
        <lang.parserDefinition
            language="ROSInterface"
            implementationClass="org.ros.clion.highlighter.RosInterfaceParserDefinition"/>

        <!-- Syntax highlighter factory (fallback) -->
        <lang.syntaxHighlighterFactory
            language="ROSInterface"
            implementationClass="org.ros.clion.highlighter.RosSyntaxHighlighterFactory"/>

        <!-- Color settings page -->
        <colorSettingsPage
            implementation="org.ros.clion.highlighter.RosColorSettingsPage"/>

        <!-- TextMate bundle provider (primary highlighting mechanism) -->
        <textmate.bundleProvider
            implementation="org.ros.clion.highlighter.RosTextMateBundleProvider"/>
    </extensions>
</idea-plugin>